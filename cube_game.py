# Импорт необходимых библиотек
import tkinter as tk  # Основная библиотека для GUI
from tkinter import ttk  # Расширенные виджеты Tkinter (хотя в коде не используется)
import random  # Для генерации случайных чисел
import time  # Для создания задержек в анимации


def roll_dice():
    """
    Функция генерации случайного числа от 1 до 6
    
    Returns:
        int: Случайное число от 1 до 6 (результат броска кубика)
    """
    return random.randint(1, 6)  # Возвращает целое число в диапазоне [1, 6]


class DiceGame:
    """
    Основной класс приложения игрового кубика.
    
    Отвечает за создание графического интерфейса и управление
    игровой логикой.
    """
    
    def __init__(self, root):
        """
        Инициализация главного окна приложения.
        
        Args:
            root (tk.Tk): Корневое окно Tkinter
        """
        self.root = root  # Сохраняем ссылку на главное окно
        self.setup_window()  # Настраиваем параметры окна
        self.create_widgets()  # Создаем все элементы интерфейса
        self.initialize_game()  # Инициализируем игровые переменные
    
    def setup_window(self):
        """Настройка параметров главного окна"""
        self.root.title("CubeGame - Бросок кубика")  # Заголовок окна
        self.root.geometry("500x600")  # Размер окна (ширина x высота)
        self.root.resizable(False, False)  # Запрет изменения размера окна
        self.root.configure(bg='#f0f0f0')  # Цвет фона (светло-серый)
    
    def create_widgets(self):
        """Создание и размещение элементов интерфейса"""
        # Заголовок приложения
        title_label = tk.Label(
            self.root,
            text="🎲 ИГРОВОЙ КУБИК 🎲",  # Эмодзи и текст заголовка
            font=("Arial", 20, "bold"),  # Шрифт: Arial, размер 20, жирный
            bg='#f0f0f0',  # Цвет фона (совпадает с фоном окна)
            fg='#2c3e50'  # Цвет текста (темно-синий)
        )
        title_label.pack(pady=20)  # Размещаем с отступом 20 пикселей сверху и снизу
        
        # Область отображения кубика
        self.create_dice_area()
        
        # Панель управления
        self.create_control_panel()
        
        # Область статистики
        self.create_stats_area()
    
    def create_dice_area(self):
        """Создание области отображения кубика"""
        # Создаем рамку для области кубика
        dice_frame = tk.Frame(
            self.root, 
            bg='#ffffff',  # Белый фон
            relief='raised',  # Рельефный бордюр
            bd=3  # Толщина бордюра (3 пикселя)
        )
        dice_frame.pack(pady=20, padx=50, fill='both', expand=True)  # Размещаем с отступами
        
        # Отображение текущего значения кубика
        self.dice_label = tk.Label(
            dice_frame,
            text="🎲",  # Начальный символ кубика
            font=("Arial", 80),  # Большой шрифт для видимости
            bg='white'  # Белый фон
        )
        self.dice_label.pack(pady=30)  # Размещаем с большим отступом
        
        # Метка результата (под кубиком)
        self.result_label = tk.Label(
            dice_frame,
            text="Нажмите кнопку для броска",  # Инструкция для пользователя
            font=("Arial", 14),  # Стандартный шрифт
            bg='white',  # Белый фон
            fg='#7f8c8d'  # Серый цвет текста
        )
        self.result_label.pack(pady=10)  # Размещаем с отступом
    
    def create_control_panel(self):
        """Создание панели управления"""
        control_frame = tk.Frame(self.root, bg='#f0f0f0')  # Рамка для кнопок
        control_frame.pack(pady=20)  # Размещаем с отступом
        
        # Основная кнопка броска
        self.roll_button = tk.Button(
            control_frame,
            text="🎲 БРОСИТЬ КУБИК 🎲",  # Текст кнопки с эмодзи
            command=self.roll_dice,  # Функция, вызываемая при нажатии
            font=("Arial", 14, "bold"),  # Жирный шрифт
            bg='#3498db',  # Синий цвет фона
            fg='white',  # Белый цвет текста
            padx=30,  # Горизонтальные отступы внутри кнопки
            pady=15,  # Вертикальные отступы внутри кнопки
            cursor='hand2',  # Изменение курсора при наведении (рука)
            relief='raised',  # Рельефный эффект
            bd=3  # Толщина бордюра
        )
        self.roll_button.pack()  # Размещаем кнопку
    
    def create_stats_area(self):
        """Создание области статистики"""
        # Создаем рамку с заголовком для статистики
        stats_frame = tk.LabelFrame(
            self.root,
            text=" 📊 СТАТИСТИКА ИГРЫ ",  # Заголовок с эмодзи
            font=("Arial", 12, "bold"),  # Жирный шрифт
            bg='#f0f0f0',  # Цвет фона
            fg='#2c3e50'  # Цвет текста
        )
        stats_frame.pack(pady=10, padx=50, fill='x')  # Размещаем с отступами
        
        # Метка для отображения статистики
        self.stats_label = tk.Label(
            stats_frame,
            text="Всего бросков: 0",  # Начальное значение
            font=("Arial", 11),  # Стандартный шрифт
            bg='#f0f0f0'  # Цвет фона
        )
        self.stats_label.pack(pady=10)  # Размещаем с отступом
    
    def initialize_game(self):
        """Инициализация игровых переменных"""
        self.roll_count = 0  # Счетчик бросков
        self.current_value = 0  # Текущее значение кубика
        
        # Словарь с символами кубиков для отображения
        # Ключи: числа от 1 до 6, значения: символы игральных костей
        self.dice_symbols = {
            1: "⚀", 2: "⚁", 3: "⚂", 
            4: "⚃", 5: "⚄", 6: "⚅"
        }
    
    def roll_dice(self):
        """
        Основная функция броска кубика с анимацией
        
        Включает генерацию случайного числа и визуальную
        анимацию вращения кубика.
        """
        # Блокировка кнопки во время анимации (чтобы избежать множественных нажатий)
        self.roll_button.config(state='disabled')
        self.result_label.config(text="Кубик вращается...")  # Обновляем текст
        
        # Воспроизведение анимации вращения
        self.animate_roll()
        
        # Генерация финального результата после анимации
        self.current_value = roll_dice()  # Используем внешнюю функцию
        
        # Обновление интерфейса с новым результатом
        self.update_display()
        self.update_stats()
        
        # Разблокировка кнопки после завершения броска
        self.roll_button.config(state='normal')
    
    def animate_roll(self):
        """
        Анимация вращения кубика
        
        Создает визуальный эффект случайного вращения
        путем быстрой смены значений.
        """
        for i in range(12):  # 12 кадров анимации
            temp_value = random.randint(1, 6)  # Случайное значение для анимации
            self.dice_label.config(text=self.dice_symbols[temp_value])  # Обновляем отображение
            self.root.update()  # Принудительное обновление интерфейса
            time.sleep(0.08)  # Задержка между кадрами (80 мс)
    
    def update_display(self):
        """Обновление отображения результата на экране"""
        # Устанавливаем финальный символ кубика
        self.dice_label.config(text=self.dice_symbols[self.current_value])
        # Показываем числовое значение
        self.result_label.config(text=f"Выпало: {self.current_value}")
    
    def update_stats(self):
        """Обновление статистики бросков"""
        self.roll_count += 1  # Увеличиваем счетчик бросков
        # Обновляем текст метки статистики
        self.stats_label.config(text=f"Всего бросков: {self.roll_count}")


def main():
    """
    Главная функция запуска приложения.
    
    Создает экземпляр Tkinter и запускает главный цикл
    обработки событий.
    """
    try:
        root = tk.Tk()  # Создаем главное окно Tkinter
        app = DiceGame(root)  # Создаем экземпляр нашего приложения
        root.mainloop()  # Запускаем главный цикл обработки событий
    except Exception as e:
        # Обработка возможных ошибок запуска
        print(f"Ошибка запуска приложения: {e}")


# Точка входа в программу
if __name__ == "__main__":
    main()  # Запускаем приложение только если файл выполняется напрямую

"""Основные особенности кода:
🎯 Сильные стороны:
Хорошая структура - логическое разделение на методы

Обработка ошибок - try/except в main функции

Блокировка интерфейса - предотвращение множественных нажатий

Анимация - плавное вращение кубика

Документация - подробные комментарии и docstrings

🔧 Архитектурные решения:
Классовая структура - инкапсуляция логики приложения

Разделение ответственности - каждый метод отвечает за свою задачу

Словарь символов - удобное управление отображением кубиков

🎨 Пользовательский опыт:
Визуальная обратная связь - анимация и изменение текста

Эмодзи - улучшают визуальное восприятие

Цветовая схема - профессиональный внешний вид

Код хорошо организован и готов к дальнейшему расширению функциональности!"""
